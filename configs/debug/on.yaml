# @package _global_

trainer:
  max_steps: 10
  val_every_n_steps: 5
  log_every_n_steps: 1
  accumulate_grad_batches: 1
  enable_checkpointing: false

datamodule:
  split:
    train: 20
    val: 4
    test: 4
  batch_size: 2
  num_workers: 0
